# ====================================
# .isdigit()
# 파이썬의 문자열 메서드
# 문자열이 숫자로만 이루어져 있는지를 검사
# 맞으면 True, 틀리면 False
# ====================================

print("123".isdigit())   # True
print("12a3".isdigit())  # False
print("abc".isdigit())   # False
print("".isdigit())      # False

# if not x.isdigit():
# x가 숫자가 아니라면

# x = int(input()) 이렇게 바로 쓰면 "abc" 입력시 에러발생
# 그래서 안전하게
x = input()

if x.isdigit():
    x = int(x)
    
# isdigit은 정수만 허용한다 . - 은 숫자로 보지 않음

# 확장방법
# 1. try-except
try:
    x = int(input())
except ValueError:
    print("숫자 입력하세요")
# 에러 처리 핵심 패턴
    
# 2. 음수까지 허용하려면
x = input()

if x.lstrip("-").isdigit():
    print("정수 가능")
# .lstrip() 문자열 왼쪽 혹은 특정 문자를 제거하는 함수

# ====================================
# generator 표현식 (제너레이터)
# ====================================

# (c in directions for c in sannpo)
# 하나씩 검사 결과를 만들어 낸다
gen = (x for x in range(3))

# 함수형태의 제너레이터

def count_up_to(n):
    i = 1
    while i <= n:
        yield i
        i += 1

gen = count_up_to(3)
print (next(gen))

# ====================================
# all()
# all(반복 가능한 것)
# 전부 True면 True, 하나라도 False면 False
# ====================================

all([True, True, True])   # True
all([True, False, True])  # False

# ====================================
# async / await
# 기다리는 작업을 효율적으로 처리하는 문법으로서
# 오래걸리는 작업(네트워크, 파일 등)을 기다리는 동안
# 다른 일을 함. 비동기 처리
# ====================================

async def f():
    await something()

# ====================================
# 리스트 컴프리핸션
# for문을 한 줄로 줄여서 리스트를 만드는 문법
# ====================================

nums = [i for i in range(5)]

nums = [i * 2 for i in range(5)]

nums = [i for i in range(10) if i % 2 == 0]

# ====================================
# enumerate()
# 인덱스 + 값 동시에 가져오기
# ====================================

arr = ['a', 'b', 'c']
for i, v in enumerate(arr):
    print (i, v)

# >> 0a , 1b , 2c

# ====================================
# zip()
# 여러개 동시에 순회
# ====================================

a = [1, 2, 3]
b = ['a', 'b', 'c']
for x, y in zip(a, b):
    print (x, y)

# >>> 1a , 2b , 3c

# ====================================
# isinstance()
# 자료형 체크, 입력 처리할 때 유용
# ====================================

x = 10
print (isinstance(x, int))  #True

# ====================================
# type()
# 자료형 학인
# ====================================

print (type([1, 2]))
#  결과 : class "list"

# ====================================
# split()
# 문자열 나누기
# ====================================

s = "abc"
print(s.split())  #['a', 'b', 'c']

# ====================================
# join()
# 리스트를 문자열로 합치는 함수
# ====================================

arr = ['a', 'b', 'c']
print (''.join(arr))   #abc
print (','.join(arr))   #a,b,c

# ====================================
# strip()
# 문자열 공백 제거 함수
# 앞, 뒤만 지울 수 있고 특정문자도 지울 수 있다
# ====================================

s = " hello"
print(s.strip())  #hello

# ====================================
# replace()
# ====================================

s = "hello"
print (s.replace('l', 'x'))  #hexxo

# ====================================
# sort(), sorted()
# 리스트 원본 변경 및 정렬, 새리스트
# ====================================

arr = [3, 2, 1]
arr.sort()
sorted_arr = sorted(arr)

arr = ['apple', 'kiwi', 'banana']
arr.sort(key = len)
print(arr)  #['kiwi' , 'apple' , 'banana']
# (key = len) : 정렬 기준을 문자열 길이로 하겠다

arr = [-5, -1, 3, -2]
arr.sort(key=abs)
# 숫자를 절댓값 기준으로 정렬

# lambda랑 쓰면 강력
arr = [(1,2), (3,1), (2,3)]

arr.sort(key = lambda x : x[1])
print (arr)

# ====================================
# get() 딕셔너리
# ====================================

d = {'a' : 1}
print(d.get('a'))   #1

print (d.get('b', 0))   #0

# b가 있으면 그 값을 줘, 없으면 0을 줘

d[x] = d.get(x, 0) +1
# x가 없으면 0부터 시작해서 1 더하기
# 있으면 그냥 그 값에 1 더하기

# ====================================
# defauldict
from collections import defaultdict
# collection이란 라이브러리에서 defaultdict를 가져오는 명령어
# 파이썬에는 기본 기능말고도 추가기능모음집(collections)가 있는데,
# 그 안에 defaultdict라는 편한 딕셔너리가 있음
# ====================================

d = defaultdict(int)
#  > 기본값이 0인 딕셔너리를 만들어라

int() > 0
d = defaultdict(int)
# 없는 키를 부르면 자동으로 0 생성

d['a'] += 1
# 딕셔너리는 비어이있기 때문에 a를 만들면 0으로 생성
# 거기에 1을 더했으니 a : 1

print(d)  # {'a' : 1}

# ====================================
#  set()
# 중복 제거
# ====================================

arr = [1, 1, 2, 2, 3]
print (set(arr))   #[1, 2, 3]

# ====================================
# sum()
# 리스트 합계 출력
# ====================================

print (sum([1, 2, 3]))   #6

# ====================================
# max(), min()
# ====================================

print (max([1, 5, 3]))   #5

# ====================================
# abs()
# 절댓값 변환
# ====================================

print(abs(-5))   #5

# ====================================
# round()
# ====================================

print(round(3.14159, 2))  #3.14
# 반올림하고 싶은 값, 소수점 아래 몇자리까지 남길지
# 그냥 소수값만 적으면 정수로 반올림
# 음수는 100단위 반올림 1234, -2  >> 1200

# ====================================
# 리스트 슬라이싱
# ====================================

arr = [1, 2, 3, 4, 5]
print(arr[1:4])   #[2, 3, 4, 5]
print(arr[::-1])  #뒤집기

# ====================================
# lambda
# 익명 함수를 만드는 파이썬 문법.
# 보통 이름없이 한 줄로 간단하게 쓰고 싶을 때 사용

# lambda 매개변수 : 반환값

# 한줄로 끝나야하며, 복잡한 함수가 필요할 경우 그냥 def 사용
# ====================================

f = lambda x : x * 2
print(f(3))   #6

# 정렬에서 많이 씀

arr = [(1, 2), (3, 1)]
arr.sort(key = lambda x : x[1])
# 각 튜플의 두번째 값을 기준으로 정렬

# ====================================
# map()
# map(함수, 리스트)
# 리스트의 각 요소를 함수에 넣어서 결과를 새 리스트로 반환
# ====================================


# ====================================
# any()
# 하나라도 True여야 True
# ====================================

arr = [True, False, False]
print (any(arr))   #True

# ====================================
# counter (빈도수 계산)
from collections import Counter
# ====================================

arr = [1, 1, 2, 3, 3, 3]

c = Counter(arr)
print(c)   #{3:3, 1:2, 2:1}

# combinations / permutations
from itertools import combinations

arr = [1,2,3]

print(list(combinations(arr, 2)))
# [(1,2), (1,3), (2,3)]

# ====================================
# range 활용 팁
# ====================================

# for i in range(5):        # 0~4
# for i in range(1, 5):     # 1~4
# for i in range(10, 0, -1):  # 역순

# ====================================
# packing
# 여러 값을 하나로 묶는 것
# ====================================

a = 1, 2, 3

a = (1, 2, 3)
# 여러 값을 튜플로 묶음 = 패킹

# ====================================
# unpacking
# 묶여 있는 값을 다시 나누는 것
# ====================================

a = (1, 2, 3)
x, y, z = a

x = 1
y = 2
z = 3
# 하나씩 꺼내서 변수에 넣음 = 언패킹

# ====================================
# 패킹, 언패킹
# ====================================

# 값 교환
a = 1
b = 2

a, b = b, a

# 일부만 받고 나머지 무시
a, b, _ = (1, 2, 3)

# *사용  (* > 나머지 전부 가져오기)
a, *b = (1, 2, 3, 4)

a = 1
b = [2, 3, 4]

# 함수에서 사용 예제
def func(x, y, z):
    print(x, y, z)

arr = (1, 2, 3)
func(*arr)

